name: GitHub Actions Demo for JAVA Program
run-name: ${{ github.actor }} is testing out GitHub Actions JAVA Program
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "I am running action-2 on push to the repo"
